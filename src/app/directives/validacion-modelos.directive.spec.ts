import { ValidacionModelosDirective } from './validacion-modelos.directive';

describe('ValidacionModelosDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidacionModelosDirective();
    expect(directive).toBeTruthy();
  });
});
